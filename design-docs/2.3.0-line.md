# 2.3.0-line

The Reset window took the name of this version. See `design-docs/2.3.0-tree-design.html`. Snack stop and the wider copy stay in that same ship.

Next coding chat. Paste from the line below. This chat ships only this version.

---

Read `/home/tiagomdv0/trait-evolution-sim/AGENTS.md` fully. Talk like a person in the chat.

This session is mechanics. I lead. Propose the change, then wait until I say apply. Do not edit `index.html` until then.

Goal: **2.3.0-line**. Two changes, one version. The parent stops feeding a kid when cling ends. The kid’s copy of the mix wobbles a bit more, and once in a while one trait jumps.

Repo: `/home/tiagomdv0/trait-evolution-sim`. Live file is `index.html`. Live version is **2.2.0-gate**. Trust the code over older notes. Archive `archive/index-2.2.0-gate.html` before the first risky edit if that snapshot is missing. Do not edit files already in `archive/`. Do not grow the Survive or Differ freeze files.

## Already true. Leave it

Births only in an open season (`BIRTH_SEASON_START` 18000, open 9000, closed 18000). Past prime (`LIFE_RISE` 3600). Bag at least `BIRTH_BAG_MIN` (20). Hunger under `BIRTH_HUNGER_MAX` (70). A miss does not move `lastBirthTick`. That stamp happens in `spawnKid`.

The kid copies the parent, then `nudgeKidTrait` wobbles each trait by `KID_NUDGE`: hunger rate 0.004, efficiency 0.08, hunt 0.06, speed 0.1, birth frequency 0.08.

After the copy, the parent keeps a dent: hunger rate up, efficiency down, speed down, one `KID_NUDGE` step, clamped. Hunt and birth frequency do not drop. Do not change that dent in this version.

Cling lasts `CLING_TICKS` (five times prime, about five minutes). The kid follows the parent. The parent does not chase. Hunger at 55 or more still aims at pellets.

`tryParentFeedKid` still gives `PARENT_FEED` (1) food whenever that parent and that kid overlap, on `BUMP_COOLDOWN` (12). Same blood line still skips knock and tax. The starter bag (about 1–4, taken off the parent) stays.

Path bar stays where I set it. Do not invent a new fill. No Help rewrite, no metrics, no theme, no birth-season edits, no gate-number edits.

## Change 1 — snack stops

In `tryParentFeedKid`, do not pass food when `kidCling(kid)` is 0 or below. The birth bag still happens in `spawnKid`. Family members still do not knock or tax each other.

What I should see: for about five minutes the kid sticks close and can still take a pellet from the parent. After that, overlap does not feed them. Some kids reach the rain. Some starve.

## Change 2 — a wider copy, and a rare jump

On the kid only. Keep the parent dent as it is.

Usual wobble becomes hunger rate ±0.004 (unchanged), efficiency ±0.08 (unchanged), hunt ±0.10, speed ±0.1 (unchanged), birth frequency ±0.12. Same clamps as now.

Once every several births, one of the five traits jumps about three times its usual wobble, then clamps. Use a fixed chance, about 1 in 8 births. No Mix slider. No new button.

What I should see: most kids still look like the parent. A few are obviously hungrier, faster, or quicker to have kids. Birth frequency should not sit on the ceiling after one generation. If it does, the jump is too kind to that trait. Say so and wait. Do not retune hunger, speed, spawn, or bump on your own.

## Ship

When I say the run feels right: bump `VERSION` and the badge to `2.3.0-line` together. MANIFEST row, README live line, a new section at the bottom of `IMPLEMENTATION_LOG.md`. Open a PR only when I ask. I prefer to commit.

Watch with me: Families or Rungs, default weather, one run. Pause, speed, Mix Apply, Reset, click a parent and a kid. Badge reads `2.3.0-line`.

Next versions, not this chat: `2.4.0-metrics`, then `2.5.0-theme`. Then this project stops.
